# CI — LoginPro WhatsApp Contingency
# Ejecuta lint, tests y build de Docker en cada push a ramas de feature y en PRs a main.

name: CI

on:
  push:
    branches:
      - main
      - 'feature/**'
  pull_request:
    branches:
      - main

# Cancela runs anteriores del mismo branch para ahorrar minutos de Actions.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ───────────────────────────────────────────────────────────────────────
  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Instalar dependencias (incluyendo devDeps para lint)
        run: npm ci

      - name: Generar Prisma Client
        run: npm run prisma:generate

      - name: Lint (ESLint)
        # Si no hay .eslintrc todavía, este step creará uno mínimo antes de correr.
        # Puedes reemplazar por tu config cuando la tengas.
        run: |
          if [ ! -f .eslintrc.json ] && [ ! -f eslint.config.js ] && [ ! -f .eslintrc.cjs ]; then
            echo '{"env":{"node":true,"es2022":true},"parserOptions":{"ecmaVersion":2022,"sourceType":"module"},"rules":{"no-unused-vars":["warn",{"argsIgnorePattern":"^_"}],"no-undef":"error","no-console":"off"}}' > .eslintrc.json
          fi
          npx eslint src/ --ext .js --max-warnings 20

  # ───────────────────────────────────────────────────────────────────────
  test:
    name: Tests
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: loginpro_test
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      NODE_ENV: test
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/loginpro_test
      SESSION_SECRET: ci-test-secret-not-for-production
      # Redis opcional en CI: los tests corren sin él (MemoryStore fallback)
      WHATSAPP_PHONE_NUMBER_ID: test_phone_id
      WHATSAPP_ACCESS_TOKEN: test_token
      WHATSAPP_VERIFY_TOKEN: test_verify

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Instalar dependencias
        run: npm ci

      - name: Generar Prisma Client
        run: npm run prisma:generate

      - name: Aplicar migraciones (base de test)
        run: npm run prisma:migrate

      - name: Correr tests
        run: npm test

  # ───────────────────────────────────────────────────────────────────────
  build:
    name: Docker Build
    runs-on: ubuntu-latest
    # Solo corre si lint y tests pasan.
    needs: [lint, test]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build imagen (sin push)
        uses: docker/build-push-action@v6
        with:
          context: .
          push: false
          tags: loginpro-wa-contingency:ci-${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          # Falla el job si la imagen no construye correctamente.
          load: true

      - name: Smoke test — healthcheck dentro del contenedor
        run: |
          # Levanta el contenedor con vars mínimas (sin DB real, solo verifica que arranca).
          docker run -d --name smoke \
            -e NODE_ENV=production \
            -e SESSION_SECRET=smoke-test-secret \
            -e DATABASE_URL=postgresql://x:x@localhost/x \
            -e WHATSAPP_PHONE_NUMBER_ID=x \
            -e WHATSAPP_ACCESS_TOKEN=x \
            -e WHATSAPP_VERIFY_TOKEN=x \
            -p 3001:3000 \
            loginpro-wa-contingency:ci-${{ github.sha }}
          sleep 5
          # El healthcheck puede fallar por DB no disponible — solo verificamos que el proceso arranca.
          docker ps --filter name=smoke --filter status=running | grep smoke || (docker logs smoke && exit 1)
          docker stop smoke
